import 'package:flutter/material.dart';

class AppColors {
  // Primary brand teal
  static const Color primary = Color(0xFF00C6AE);
  static const Color primaryDark = Color(0xFF009E8D);
  static const Color primaryLight = Color(0xFF4DDECE);

  // Background
  static const Color background = Color(0xFF0D1117);
  static const Color surface = Color(0xFF161B22);
  static const Color surfaceVariant = Color(0xFF21262D);
  static const Color surfaceElevated = Color(0xFF2D333B);

  // Status Colors
  static const Color statusReported = Color(0xFFEF4444);
  static const Color statusInProgress = Color(0xFFF59E0B);
  static const Color statusFixed = Color(0xFF22C55E);

  // Severity Colors
  static const Color severityHigh = Color(0xFFFF3B30);
  static const Color severityMedium = Color(0xFFFF9F0A);
  static const Color severityLow = Color(0xFF30D158);

  // Text
  static const Color textPrimary = Color(0xFFE6EDF3);
  static const Color textSecondary = Color(0xFF8B949E);
  static const Color textTertiary = Color(0xFF484F58);

  // Border
  static const Color border = Color(0xFF30363D);
  static const Color borderLight = Color(0xFF21262D);

  // Error
  static const Color error = Color(0xFFF85149);

  // Map Marker Colors
  static const Color markerReported = Color(0xFFEF4444);
  static const Color markerInProgress = Color(0xFFF59E0B);
  static const Color markerFixed = Color(0xFF22C55E);
}
